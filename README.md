# Setup

## Linux

Install the `task` utility :
```
sudo snap install task --classic
```

or, for Arch BTW
```
yay -S go-task-bin
```

## MacOS

Install the `task` utility :
```
brew install go-task
```
